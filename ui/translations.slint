// ============================================================================
// Translations
// ============================================================================

export global Translations {
    in-out property <int> language: 0; // 0: Chinese, 1: English
    
    // Navigation
    out property <string> nav_fonds: language == 0 ? "📁 全宗" : "📁 Fonds";
    out property <string> nav_schema: language == 0 ? "⚙️ 分类方案" : "⚙️ Schema";
    out property <string> nav_classification: language == 0 ? "🗂 分类体系" : "🗂 Classification";
    out property <string> nav_settings: language == 0 ? "🔧 设置" : "🔧 Settings";
    out property <string> nav_files: language == 0 ? "📂 文件" : "📂 Files";
    
    // Fonds Management
    out property <string> btn_add_fonds: language == 0 ? "添加全宗" : "Add Fonds";
    out property <string> btn_delete: language == 0 ? "删除" : "Delete";
    
    // Schema Management
    out property <string> label_schema: language == 0 ? "分类方案" : "Schema";
    out property <string> label_items: language == 0 ? "项目" : "Items";
    out property <string> btn_add: language == 0 ? "添加" : "Add";

    // Classification Management
    out property <string> label_top_classifications: language == 0 ? "一级分类" : "Top Level";
    out property <string> label_child_classifications: language == 0 ? "二级分类" : "Children";
    
    // Files Management
    out property <string> label_series: language == 0 ? "案卷" : "Series";
    out property <string> label_files: language == 0 ? "文件" : "Files";
    out property <string> btn_refresh: language == 0 ? "刷新" : "Refresh";
    out property <string> btn_open: language == 0 ? "打开" : "Open";
    
    // Settings
    out property <string> label_language: language == 0 ? "语言" : "Language";
    out property <string> label_archive_libraries: language == 0 ? "档案库" : "Archive Libraries";
    out property <string> btn_add_library: language == 0 ? "添加库" : "Add Library";
    out property <string> btn_remove_library: language == 0 ? "删除库" : "Remove Library";
    out property <string> menu_rename: language == 0 ? "重命名" : "Rename";
    out property <string> menu_delete: language == 0 ? "删除" : "Delete";
    out property <string> menu_activate: language == 0 ? "激活" : "Activate";
    out property <string> menu_deactivate: language == 0 ? "停用" : "Deactivate";
    out property <string> btn_export: language == 0 ? "导出" : "Export";
    out property <string> btn_import: language == 0 ? "导入" : "Import";
    out property <string> btn_apply: language == 0 ? "应用" : "Apply";
    out property <string> btn_cancel: language == 0 ? "取消" : "Cancel";
}
